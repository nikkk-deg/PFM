num.toString(base) - возвращает строковое представление числа в системе счисления base

Math.floor() - округление в меньшую сторону
Math.ceil() - округление в большую сторону
Math.round() - округление для ближайщего целого
Math.trunc() - отбрасывает дробную часть

num.toFixed(n) - возвращает строку, округляет число до n-го знака после запятой

isNan(value) - проверяет является value NaN или нет

isFinite(value) - пробразует аргумент в число и возвращает true, если оно является обычнвм числом, не NaN/Infinity/-Infinity

Object.is(a,b), тоже самое сто и ===, но работает с NaN, и не работает с 0 -0

parseInt(x, radix)   -  читаю число из строки, если находят не числовой символ, то возращают все 
parseFloat(x, radix) -  что было до этого символа. radx - число может считать числа в разных системах счисления и функция преобразует все в 10 систему. ( parseInt('100px') ); // 100, вернет NaN, если не смогли проситать ни 1 цифру