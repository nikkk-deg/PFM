arr.push(items) - добавть эл-т(ы) в конец массивы
arr.pop() - удаляет и возвращает последний эл-т массиваarr.push() - добавляет эл-т в конец строки
arr.shift() - удаляет из массива первый эл-т и возвращает его
arr.unshift(items) - добавляет эл-т(ы) в начало массива

let arr = [1, 2, 3];
String(arr) === '1,2,3'; // true

let arr = new Array(5) - создать массив длинной 5, равносильно: let arr = [];
																arr.lenth = 5;

специальный цикл для массивов for of																